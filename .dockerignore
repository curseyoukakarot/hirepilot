# Exclude everything by default
*

# Include only what the backend API needs
!backend/
!backend/**
!shared/
!shared/**

# Include root-level files that might be required
!package.json
!package-lock.json
!.env.*
!Dockerfile
!.dockerignore

# Explicitly exclude heavy/unneeded stuff inside backend
backend/node_modules/
backend/dist/
backend/.tsbuildinfo
backend/test/